namespace: platform-dev

replicacCount: 2

image:
  repository: orbit-api
  tag: "0.1.1"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

config:
  appName: orbit-api
  message: "Gitops with Argo"

secret:
  orbitSecret: "not-for-production"

probes:
  readiness:
    path: /readyz
    initialDelaySeconds: 3
    periodSeconds: 5
  
  liveness:
    path: /healthz
    initialDelaySeconds: 3
    periodSeconds: 10